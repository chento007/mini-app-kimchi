import React from "react";
import { IProduct } from "../../../.types/prdoduct";

interface Props {
  products: IProduct[];
}

export const ListCardProduct: React.FC<Props> = ({ products }) => {
  return <div></div>;
};
